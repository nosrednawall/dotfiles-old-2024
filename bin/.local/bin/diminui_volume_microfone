#!/bin/bash

pactl set-source-volume @DEFAULT_SOURCE@ -5%
volume=$(pactl get-source-volume @DEFAULT_SOURCE@ | grep "Volume" | cut -b 29,30,31,32,33)
notify-send -t 3000 "Volume microfone $volume" "Diminuído 5% do volume do microfone"
