#!/bin/bash

pactl set-sink-volume @DEFAULT_SINK@ -5%
volume=$(pactl get-sink-volume @DEFAULT_SINK@ | grep "Volume" | cut -b 29,30,31,32,33)

notify-send -t 3000 "Volume $volume" "Diminuído 5% do volume"
